# render_news.py config: python3 templates/render_news.py --out apps/Kazumi
# Colors left unset are auto-derived: tint from config.toml, background as
# a light shade of the icon's dominant color.

name = "Kazumi"
tagline = "基于自定义规则的番剧采集与在线观看"

# Color notes — each key below controls one element of the image. All are
# optional: unset keys are auto-derived. Shown values are the current result.
[colors]
# tint = "#4CAF50"          # NEW UPDATE badge + top-left glow (default: [app] tint_color)
# tint_alt = "#4CAF50"      # bottom-right glow (default: [source] tint_color)
# background = "#E0EBD6"    # gradient base (derived from the icon's dominant color)
# bg_mid = "#EDF3E7"        # gradient mid stop (derived from background)
# bg_dark = "#CBDEBB"       # gradient end stop (derived from background)
# text_color = "#111111"    # app name (auto white/black)
# tagline_color = "#85BD51" # tagline text (derived from background)
