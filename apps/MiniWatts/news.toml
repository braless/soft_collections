# render_news.py config: python3 templates/render_news.py --out apps/MiniWatts
# Colors left unset are auto-derived: tint from config.toml, background as
# a light shade of the icon's dominant color.

name = "MiniWatts"
tagline = "Battery, charging and thermal instrument for iPhone"

# Color notes — each key below controls one element of the image. All are
# optional: unset keys are auto-derived. Shown values are the current result.
[colors]
# tint = "#30F040"          # NEW UPDATE badge + top-left glow (default: [app] tint_color)
# tint_alt = "#30F040"      # bottom-right glow (default: [source] tint_color)
# background = "#D6EBD7"    # gradient base (derived from the icon's dominant color)
# bg_mid = "#E7F3E8"        # gradient mid stop (derived from background)
# bg_dark = "#BBDEBC"       # gradient end stop (derived from background)
# text_color = "#111111"    # app name (auto white/black)
# tagline_color = "#51BD56" # tagline text (derived from background)
