# render_news.py config: python3 templates/render_news.py --out apps/Mangayomi
# Colors left unset are auto-derived: tint from config.toml, background as
# a light shade of the icon's dominant color.

name = "Mangayomi"
tagline = "Read manga, novels, and watch anime"

# Color notes — each key below controls one element of the image. All are
# optional: unset keys are auto-derived. Shown values are the current result.
[colors]
# tint = "#EF4444"          # NEW UPDATE badge + top-left glow (default: [app] tint_color)
# tint_alt = "#EF4444"      # bottom-right glow (default: [source] tint_color)
# background = "#EBD6D8"    # gradient base (derived from the icon's dominant color)
# bg_mid = "#F3E7E8"        # gradient mid stop (derived from background)
# bg_dark = "#DEBBBE"       # gradient end stop (derived from background)
# text_color = "#111111"    # app name (auto white/black)
# tagline_color = "#BD515C" # tagline text (derived from background)
